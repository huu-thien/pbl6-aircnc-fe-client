export type LoginData = { username: string; password: string };
export type RegisterData = {
  fullName: string;
  email: string;
  username: string;
  password: string;
  confirmPassword?: string;
};
